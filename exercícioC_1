#include <stdio.h>
#include <stdlib.h>


int main(){

    int num;

    printf("Digite um numero: ");
    scanf("%d", &num);

    if(num % 2 == 0){
        printf("%d, numero par. \n", num);
    }else{
        printf("%d, numero impar. \n", num);
    }
    
    return 0;
}
