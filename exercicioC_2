#include <stdio.h>
#include <stdlib.h>


int main(){

    int num;
    int soma = 0;

    printf("Digite um numero: ");
    scanf("%d", &num);

    for(int i = 1; i <= num; i++){
        soma += i;
    }

    printf("A soma de  1 a %d = %d\n", num, soma);

    return 0;
}
